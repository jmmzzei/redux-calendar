import styled from "styled-components"

export const HomeBtnStyled = styled.button`
  width: 3.5rem;
  height: 3.5rem;
  cursor: pointer;
  border: none;
  border-radius: 50%;
  margin: 3rem;
  background: var(--light);
  color: var(--dark);
`
