import styled from "styled-components"

export const NavbarStyled = styled.nav`
  color: var(--light);
  background: var(--dark);
  user-select: none;

  display: flex;
  align-items: center;
  justify-content: center;

  height: 5rem;
  margin: 0;
  padding: 4rem 0;

  p {
    text-align: center;
    width: 90%;
    font-size: 3rem;
    font-weight: bold;
  }
`
